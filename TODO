THIS SYSTEM IS NOW INTENDED TO BE MERGED WITH GYPSUM & PIKES

Filesystem
	ZFS/Lustre on SSDs for Scratch (auto symlink)
	Old resources
	NESE

OpenOnDemand
	config/branding
	auth
	ssh key management
	xdmod
	jupyter interface

slurm
	slurm.conf (all)
	cgroup & gres (head)
	daemons

xdmod
	access to slurmdb via shredder
	coordination with website

firewall & networking
	static assignment (head)
	dhcpd.conf (head)
	hosts (all)

management plays
	update single compute
	update all compute
	update head
	restart specific services
	filesystem allocations
