Filesystem
	ZFS/Lustre on SSDs for Scratch (auto symlink)
	NESE
Website
	ssh key management
	filesystem browser?
	xdmod
	jupyter interface
	webserver & daemon
Auth
	Shibboleth
	Web users
	Real users

Jupyterhub config
	slurmspawner in config
	wrapspawner?
	daemon

slurm
	slurm.conf (all)
	cgroup & gres (head)
	daemons

xdmod
	access to slurmdb via shredder
	coordination with website

firewall & networking
	static assignment (head)
	dhcpd.conf (head)
	hosts (all)

management plays
	update single compute
	update all compute
	update head
	restart specific services
	filesystem allocations
